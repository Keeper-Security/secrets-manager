name: Test .NET Storage - Oracle KMS

on:
  push:
    branches:
      - 'release/storage/dotnet/oracle-kms/**'
    paths:
      - 'sdk/dotNet/Integrations/OracleKeyManagement/**'
      - '.github/workflows/test.dotnet.storage.oracle.kms.yml'
  pull_request:
    branches:
      - 'release/storage/dotnet/oracle-kms/**'
    paths:
      - 'sdk/dotNet/Integrations/OracleKeyManagement/**'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./sdk/dotNet/Integrations/OracleKeyManagement
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup .NET SDK
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '9.0.x'

      - name: Restore dependencies
        run: dotnet restore

      - name: Build
        run: dotnet build --configuration Release --no-restore
