name: Publish to Maven (Java Storage AWS KMS)
on:
  workflow_dispatch:
    inputs:
      publish:
        description: 'Publish to Maven Central (uncheck to build only)'
        required: false
        default: true
        type: boolean

jobs:
  publish:
    uses: ./.github/workflows/reusable.maven.central.publish.yml
    with:
      working-directory: ./sdk/java/storage/keeper_secrets_manager_storage_aws_kms
      project-name: keeper-secrets-manager-java-storage-aws-kms
      project-title: Keeper Secrets Manager AWS KMS Storage
      java-version: '8'
      publish: ${{ github.event.inputs.publish == 'true' }}
    secrets: inherit
