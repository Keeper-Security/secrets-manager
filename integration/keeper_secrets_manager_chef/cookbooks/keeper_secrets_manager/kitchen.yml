driver:
  name: docker

provisioner:
  name: chef_zero

platforms:
  - name: ubuntu-22.04

suites:
  - name: default
    run_list:
      - recipe[keeper_secrets_manager_chef::default]
    verifier:
      inspec_tests:
        - test/integration/default