---
requires_ansible: '>=2.10'
plugin_routing:
  action:
    keeper_copy:
      redirect: keepersecurity.keeper_secrets_manager.keeper_copy
    keeper_get:
      redirect: keepersecurity.keeper_secrets_manager.keeper_get
    keeper_set:
      redirect: keepersecurity.keeper_secrets_manager.keeper_set
    keeper_cleanup:
      redirect: keepersecurity.keeper_secrets_manager.keeper_cleanup
  lookup:
    keeper:
      redirect: keepersecurity.keeper_secrets_manager.keeper
action_groups:
  keeper_secrets_manager:
    - keeper_copy
    - keeper_get
    - keeper_set
    - keeper_cleanup
